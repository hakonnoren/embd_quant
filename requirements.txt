sentence-transformers>=2.2.0
datasets>=2.14.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0
torch>=2.0.0
tqdm>=4.65.0
tabulate>=0.9.0
einops>=0.7.0
matplotlib>=3.7.0

# Optional: optimized search backends (uncomment as needed)
# faiss-cpu              # CPU-optimized similarity search
# faiss-gpu              # GPU-optimized (requires CUDA, install via conda: conda install -c pytorch faiss-gpu)
